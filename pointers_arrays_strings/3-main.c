/**
 * main - Main function to print a string
 *
 * Return: 0 if successful, other value otherwise.
 */
#include "main.h"

int main(void)
{
    char *str;

    str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
    _puts(str);
    return (0);
}
